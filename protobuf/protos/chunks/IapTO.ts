import { CMSField } from "../../interfaces/CMSField";

export const IapTO: Map<number, CMSField> = new Map([
  [
    2,
    {
      name: "iap",
      type: "group",
      fields: new Map([]),
    },
  ],
]);
